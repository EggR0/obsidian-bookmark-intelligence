services:
  billing:
    build:
      context: ..
      dockerfile: server/Dockerfile
    restart: unless-stopped
    env_file:
      - billing.env
    ports:
      - "127.0.0.1:8787:8787"
    volumes:
      - billing-data:/data

volumes:
  billing-data:
